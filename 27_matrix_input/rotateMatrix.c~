#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

int checkinput(FILE * f){
  int c = 0;
  int col = 0;
  c = fgetc(f);
  while (!(c == EOF)){
    for(int i = 0;  i < 10; i++){
      if (c == '\n'){
	fprintf(stderr, "char error");
	return 0;
      }
      c = fgetc(f);
    }
    if (c != '\n'){
    }
  }
}
